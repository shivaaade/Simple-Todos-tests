// Write your code here

const TodoItem = propse => {
  const {userTitle} = propse
  const {id, title} = userTitle

  return (
    <li>
      <p>{titile}</p>
      <button>Delete</button>
    </li>
  )
}

export default TodoItem
